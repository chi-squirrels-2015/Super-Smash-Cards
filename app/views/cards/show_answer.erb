<div class="container">
  <h1><%="OH SHIGGITY SNAP, UR WRONG" if @wrong %></h1>
  <h2><%= @card.question %></h2>
  <h2><%= @card.answer %></h2>

  <a href="/<%= @game.user.username %>/games/<%= @game.id %>"><input type="submit" value="Flip"></a>
  <a href="/<%= @game.user.username %>/games/<%= @game.id %>/skip"><input type="submit" value="Skip"></a>
</div>
